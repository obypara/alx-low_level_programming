#include "main.h"
#include <string.h>
/**
 * _strcmp -a function that compares two strings
 * @s1: first param
 * @s2: second param
 *
 * Return: integer
 */
int _strcmp(char *s1, char *s2)
{
	int i;

	i = strcmp(s1, s2);
	return (i);
}
