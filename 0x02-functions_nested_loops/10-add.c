#include "main.h"
/**
 *add - a function that adds two integers
 *@i:integer to be added
 *@c: second integer to be added
 *
 *Return: the result.
 */
int add(int i, int c);
{
	int i, c;

	int add = i + c
		for (int add)
		{
			return (add);
		}
}
